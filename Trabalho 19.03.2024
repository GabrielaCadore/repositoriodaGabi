1) implemente o algoritmo de ordenação Bubble Sort usando python
def bubble_sort(arr):
    n = len(arr)
    # Percorre todos os elementos do array
    for i in range(n):
        # Últimos i elementos já estão no lugar certo, então não precisamos
        # considerá-los novamente. -1 é usado porque queremos comparar elementos adjacentes.
        for j in range(0, n-i-1):
            # Percorre o array do início até o fim - 1 - i
            # Troca se o elemento atual for maior que o próximo
            if arr[j] > arr[j+1]:
                arr[j], arr[j+1] = arr[j+1], arr[j]

# Teste
arr = [64, 34, 25, 12, 22, 11, 90]
bubble_sort(arr)
print("Array ordenado:")
for i in range(len(arr)):
    print("%d" % arr[i])




    2) implemente um algorítimo que imprima apenas as vogais de uma
determinada palavra ou frase

def imprime_vogais(frase):
    vogais = "aeiouAEIOU"
    vogais_na_frase = [letra for letra in frase if letra in vogais]
    return ''.join(vogais_na_frase)

# Teste
frase = "Olá, mundo! Esta é uma frase de exemplo."
print("Vogais na frase:", imprime_vogais(frase))
